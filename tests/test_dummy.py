def test_placeholder():
    """
    A placeholder test so pytest doesn't fail with exit code 5 (no tests collected)
    during git pre-commit hooks when there are no other tests.
    """
    assert True
